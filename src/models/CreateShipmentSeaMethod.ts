// =========================================================================================
//
// This file is AUTO-GENERATED by https://github.com/lune-climate/openapi-typescript-codegen
//
// In most cases you DON'T WANT TO MAKE MANUAL CHANGES to it – they WILL BE OVERWRITTEN.
//
// =========================================================================================

/* istanbul ignore file */
/* tslint:disable */
/* eslint-disable */

import type { FuelConsumption } from './FuelConsumption.js'
import type { SimpleSeaShippingMethod } from './SimpleSeaShippingMethod.js'
import type { TradeLane } from './TradeLane.js'

/**
 * Sea transport method accepted when creating a shipment.
 *
 * At least one of `vessel_imo_number`, `vessel_type`,
 * `vessel_tracking`, or `vessel_inference` must be supplied.
 * The backend selects the most specific identification path
 * available from the supplied combination:
 * 1. `vessel_imo_number` identifies a vessel directly.
 * 2. `vessel_inference` (carrier schedule + dates) is used when no
 * IMO number is provided.
 * 3. `vessel_tracking` (MMSI + dates) is used as a fallback.
 * 4. `vessel_type` + optional `trade_lane` / `refrigerated` cover
 * generic sea transport without a specific vessel.
 *
 * Providing conflicting fields (e.g. both `vessel_tracking` and
 * `vessel_inference`) is permitted; the highest-priority path above
 * is used and the remaining fields are ignored.
 *
 */
export type CreateShipmentSeaMethod = {
    /**
     * The vessel's IMO number without the `IMO` prefix.
     *
     */
    vesselImoNumber?: string
    vesselType?: SimpleSeaShippingMethod | 'container_ship'
    /**
     * Name of the vessel.
     */
    vesselName?: string
    /**
     * Voyage number.
     */
    voyageNumber?: string
    /**
     * A container transport is either refrigerated or dry.
     *
     */
    refrigerated?: boolean
    tradeLane?: TradeLane
    vesselTracking?: {
        departureOn: string
        arrivalOn: string
        mmsiNumber?: string
    }
    vesselInference?: {
        departureOn: string
        arrivalOn: string
        /**
         * The carrier used for shipment.
         */
        carrierName: CreateShipmentSeaMethod.carrier_name
    }
    /**
     * Primary fuel consumption for this leg.
     */
    fuelConsumption?: FuelConsumption
    /**
     * Share of biofuel in the fuel mix (0 to 1).
     */
    biofuelShare?: string | null
}

export namespace CreateShipmentSeaMethod {
    /**
     * The carrier used for shipment.
     */
    export enum carrier_name {
        ALIAN_A = 'ALIANÇA',
        ANL = 'ANL',
        CMA_CGM = 'CMA CGM',
        CNC = 'CNC',
        COSCO_SHIPPING = 'COSCO SHIPPING',
        CROWLEY = 'CROWLEY',
        EVERGREEN = 'EVERGREEN',
        GOLD_STAR_LINE = 'GOLD STAR LINE',
        HAIHUA_SHIPPING = 'HAIHUA SHIPPING',
        HAMBURG_S_D = 'HAMBURG SÜD',
        HAPAG_LLOYD = 'HAPAG-LLOYD',
        HMM = 'HMM',
        ICL = 'ICL',
        JINJIANG_SHIPPING = 'JINJIANG SHIPPING',
        KAMBARA_KISEN = 'KAMBARA KISEN',
        KOREA_MARINE_TRANSPORT = 'KOREA MARINE TRANSPORT',
        MAERSK = 'MAERSK',
        MATSON = 'MATSON',
        MCC = 'MCC',
        MSC = 'MSC',
        NAMSUNG = 'Namsung',
        ONE = 'ONE',
        OOCL = 'OOCL',
        PIL = 'PIL',
        SEAGO = 'SEAGO',
        SEALAND = 'SEALAND',
        SITC = 'SITC',
        SM_LINE = 'SM LINE',
        WAN_HAI = 'WAN HAI',
        YANG_MING = 'YANG MING',
        ZIM = 'ZIM',
    }
}
